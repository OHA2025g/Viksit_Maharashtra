# Local dev — backend origin (no /api suffix)
REACT_APP_BACKEND_URL=http://localhost:8001
WDS_SOCKET_PORT=3000

# Production (EasyPanel frontend service env) — pick ONE:
#
# A) Same domain /api proxy (recommended):
# API_UPSTREAM=http://maharashtra_ai_viksitmaharashtra_backend:8001
#
# B) Public backend URL (cross-origin; backend CORS must allow frontend):
# BACKEND_URL=https://YOUR-BACKEND-PUBLIC-URL
