# Local dev — backend origin (no /api suffix)
REACT_APP_BACKEND_URL=http://localhost:8001
WDS_SOCKET_PORT=3000

# Production (EasyPanel frontend service env):
# BACKEND_URL=https://YOUR-BACKEND-PUBLIC-URL
# or REACT_APP_BACKEND_URL=... (same value)
# Leave empty only if you proxy /api on the same domain to the backend.
