# Local development (yarn start) — baked in at dev server start
REACT_APP_BACKEND_URL=http://localhost:8001

# EasyPanel / Docker (runtime, no rebuild needed):
# REACT_APP_BACKEND_URL=https://api.your-domain.com
# or BACKEND_URL=https://api.your-domain.com
